Warframe data visualizer in a pretty GUI.

Optimized for Windows, not tested fully on Linux/Mac but potentially working.

## How to Build

Build with PyInstaller

PyInstaller --noconfirm --onefile "Warframe Alert.spec"
